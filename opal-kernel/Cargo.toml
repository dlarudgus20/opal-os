[package]
name = "opal-kernel"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]
test = false
bench = false
harness = false

[dependencies]
opal-ktest = { path = "../opal-ktest" }

[build-dependencies]
opal-build = { path = "../opal-build" }
