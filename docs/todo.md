# TODO

## 리소스 내장 방식 개선 (`.c` 대체)
- 목표: 폰트/리소스 내장 방식을 `.c` 생성 중심에서 바이너리 리소스 중심으로 정리
- 후보 방식:
  - `#embed` 직접 사용
  - `objcopy` 또는 `ld -b binary`로 바이너리 섹션 연결
- 완료 기준:
  - 리소스 변경 시 빌드 경로 단순화
  - 리소스 재생성/의존성 관리 명확화
  - 기존 커널 부팅/출력 동작 유지

## 화면 출력 속도 개선
- 목표: framebuffer/TTY 출력 경로의 체감 지연 감소
- 우선 점검 대상:
  - `fb_draw_char`
  - 문자열 출력 경로의 반복 호출/분기 비용
  - 스크롤 시 메모리 이동 비용
- 개선 후보:
  - glyph 캐시/사전 전개
  - `memcpy` 기반 blit 최적화
  - dirty region 갱신
  - 문자열 단위 일괄 출력
- 완료 기준:
  - 긴 로그 출력 시 기존 대비 성능 개선 수치 확보
  - 가독성/정확성 회귀 없음

## 태스크/스케줄링 기초 도입
- 목표: 최소 커널 태스크 실행 기반 마련
- 범위:
  - task 구조체
  - 컨텍스트 저장/복원
  - idle task
  - round-robin 스케줄러(기본형)
- 완료 기준:
  - 2개 이상 태스크가 교대로 실행됨을 확인
  - 예외/패닉 경로와 충돌 없이 동작
  - 디버그 로그로 스케줄링 추적 가능
