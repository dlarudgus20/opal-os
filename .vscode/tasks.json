{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clangd: generate compile_commands.json",
            "type": "shell",
            "command": "LC_ALL=C compiledb --full-path -n make -B"
        }
    ]
}
